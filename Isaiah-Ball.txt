Assignment 1 Url
https://codepen.io/zay-ownlane-mmg/pen/MWmzRVB

   1. What is Semantic HTML?

A semantic element clearly describes its meaning to both the browser and the developer.

2. What is HTML used for?

HTML is the language for describing the structure of Web pages.
 
    3. What is an attribute and where do we put it?
provides additional information about an element,placed inside the opening tag of the element.
 
    4. What is the h1 tag used for? How many times should I use it on a page?

H1 is used for the title, it can be used unlimited times but should only be used once.

    5. Name two tags that have required attributes

<img src="" alt=""> img tag <a href=""> a tag

    6. What do we put in the head of our HTML document? 
All the metadata such as document title, character set, styles, scripts, and other meta information.

    7. What is an id? 
Id defines an identifier which must be unique(only1)throught the whole system.

    8. What elements can I add an id to?

 semantic elements one time 

Side note: I'm not sure if your hammering home the fact that the Id attribute can only be used once with each element. 
But even saying that confuses me because it can be used as bookmarks with different tags such as h1 id on the 
same document so it's a tricky question. 
 
    9. How many times can I use the same id on a page? 

1 time

    10. What is a class? 

An attribute which specifies one or more class names for an HTML element

    11. What elements can I add a class to?
 All  elements.

    12. How many times can I use the same class on a page? 
unlimited
    13. How do I get my link to open in a new tab?

<a href="url" target="blank"> attribute to link<a>

    14. What is the alt attribute in the image tag used for? 

Adds a text description to an image on a Web page

    15. How do I reference an id?

#id name 

    16. What is the difference between a section and a div

Sections contain grouped material. Div's can be specified at user's discretion with no conveyed meaning originally.  

    17. What is CSS used for? 

Styling of webpage

    18. How do we select an element? Example - every h2 on the page
simply using the h2 selector/    h2 {}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
 ID is only used to identify one single element in HTML

    20. How do we select classes in CSS?
.classname {}
    21. How do we select a p element with a single class of “human””?
p.human {}
    22. What is a parent child selector? When would this be useful? 

selects all elements that are a direct child of the specified element.
If a element doesn't have styling the child attribute inherits it from the parent.

    23. How do you select all links within a div with the class of sidebar?
div.sidebar {}
    24. What is a pseudo selector?
 a selector that selects elements that are in a specific state(first element of type)or being hovered over.

    25. What do we use the change the spacing between lines?
line-height property

    26. What do we use to change the spacing between letters?
letter-spacing property

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
text-transform css porperty

    28. How do I add a 1px border around my div that is dotted and black?
.div { border: dotted 1px black; }

    29. How do I select everything on the page? 
*{}
    30. How do I write a comment in CSS?
/* */
    31. How do I find out what file I am in, when I am using the command line? 
pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
rm filename
you can accidently remove the entire folder its stored in as well depending on how you name your files if you don't know your current location in Gitbash (i.e pwd)
    34. Why should I use version control? 
because it allows multiple versions of the same file to be altered without risk of corrupting the original code.
    35. How often should I commit to github?
After every step. Every 4-5 hours
    36. What is the command we would use to push our repo up to github? 
git push /// literally!

    37. Walk me through Lambda's git flow. 
Creating a forked copy  creating a folder through "git", Isaiah-Ball, copying the url to task's 1 assignment and
pasting it inside the folder or git repository


Stretch Questions

    1. What is the difference between an inline element and a block element?
Inline elements do not force a new line to begin in the document flow. Block elements do, although this can be changed with Css styling.
    
2. What happens when an element is positioned absolutely?
It sits on its own layer separate from everything else.
 
3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?
element{ height:auto; width__px; }
 
4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
<address> <article> <div> // <a> <abbr> <code> // <button>
5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?
 
The box model explains how the area inside and outside the box can be allocated for the most functional html;aka Aesthetics. 

My solution so far is//  box-sizing: border-box; padding:auto; <div class="container"> .container{ align-items:center; } * {paddding:auto; }
box-sizing: content-box;  I have multiple tools to getting to the desired outcome, but i'm unsure of 1 answer.
